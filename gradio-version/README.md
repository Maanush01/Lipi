# Lipi (Gradio version)

The original local version of Lipi, built with Gradio.
Not deployed, since free Hugging Face Spaces no longer supports Gradio hosting.
See the main `streamlit_app.py` for the deployed version.
